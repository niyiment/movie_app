name: movie_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  cached_network_image: ^3.4.1
  dio: ^5.9.0
  flutter:
    sdk: flutter
  flutter_dotenv: ^6.0.0
  flutter_riverpod: ^3.0.3
  go_router: ^17.0.0
  intl: ^0.20.2
  json_annotation: ^4.9.0
  logger: ^2.6.2
  retrofit: ^4.9.0
  riverpod: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.7.1
  retrofit_generator: ^10.1.3
  json_serializable: ^6.11.1

flutter:
  uses-material-design: true

  assets:
    - assets/screenshots/
    - .env

